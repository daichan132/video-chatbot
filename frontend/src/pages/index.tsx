import { Layout } from '@/components/layout';
import { Chatbot } from '@/features/Chatbot';

const Home = () => {
  return (
    <Layout>
      <Chatbot />
    </Layout>
  );
};

export default Home;
